# Git files
.git
.gitignore

# Python cache and build artifacts
__pycache__/
*.pyc
*.pyo
*.pyd
*.so
*.egg
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
env/
ENV/

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Documentation (exclude from build context unless needed at runtime)
README.md
*.md
docs/

# Logs and temp files
*.log
logs/
tmp/
temp/
*.tmp

# OS junk
.DS_Store
Thumbs.db

# Test files
tests/
test_*/
*_test.py

# CI/CD configs (very important to exclude – they can be large)
.github/
.gitlab/
.gitlab-ci.yml

# Local config and secrets
.env
.env.local
*.local

# Docker-specific (avoid sending large or irrelevant files)
Dockerfile
docker-compose.yml
*.dockerignore

# Your specific project files that are NOT needed in the image
# (Only src/, wrapper.py, config_macd.json, and requirements.txt should be sent)
# Exclude everything else at root if not explicitly needed
# But better: explicitly allow what you need via selective COPY in Dockerfile (already done)

# Optional: exclude large data or cache directories if you ever add them
cache/
data/
*.csv
*.jsonl
*.db