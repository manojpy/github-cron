# =============================================================================
# Core Dependencies - Tested with Python 3.11
# =============================================================================

# Async HTTP client with C speedups for better performance
aiohttp[speedups]==3.10.11

# High-performance async event loop (30-40% faster than asyncio)
uvloop==0.22.1

# Data processing
pandas==2.2.3
numpy==2.1.3

# Redis client with hiredis C parser (10x faster parsing)
redis[hiredis]==5.2.1

# Data validation and settings management
pydantic==2.10.3

# System and process utilities
psutil==6.1.0
orjson==3.9.15

# Timezone database
tzdata==2024.2

# Type hints compatibility
typing-extensions==4.12.2

# Prometheus metrics (optional but recommended)
prometheus-client==0.21.0